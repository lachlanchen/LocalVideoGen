.venv/
.downloads/
.ruff_cache/
.pytest_cache/
__pycache__/
*.py[cod]

# Private user IP: prompts, settings, sessions, generated evidence, uploads,
# browser state, logs, job history, and verification receipts never belong in Git.
runtime/

# Upstream working copies and all ComfyUI inputs/outputs are local-only.
ComfyUI/
workflow_templates/
disabled_custom_nodes/

# Never publish model weights, resumable downloads, or local databases.
*.safetensors
*.ckpt
*.gguf
*.pt
*.pth
*.aria2
*.sqlite3
*.db

# Local secrets and generated diagnostics.
.env
.env.*
*.log
